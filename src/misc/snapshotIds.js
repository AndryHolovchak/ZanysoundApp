import { generateId } from "../utils/idUtils";

let searchScreenSnapshotId = generateId();
let favoritesScreenSnapshotId = generateId();
let recommendedScreenSnapshotId = generateId();
let profileScreenSnapshotId = generateId();

export {
  searchScreenSnapshotId,
  favoritesScreenSnapshotId,
  recommendedScreenSnapshotId,
  profileScreenSnapshotId,
};
